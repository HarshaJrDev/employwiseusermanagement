

After login, you will be redirected on the page of users
You can see all users with pages
You can find users by name or email
You can edit or delete users

 Reqres API is a mock aPI and some operations such as puts and deletes do not actually update or delete data on the server. Changes are only reflected on client-side.
 Login tokens are stored in localistrages for firmness.
The user search is applied to client-side because API search does not support functionality.
 The application is responsible and works well on both desktop and mobile devices.
 Error handling is applied for API requests and form verification.